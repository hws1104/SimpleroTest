<div class="mx-auto w-5/6">
  <div class="flex justify-between items-center mt-5">
    <h1 class="font-bold text-3xl">
      Editing comment
    </h1>
  </div>

  <div class="min-w-full">
    <%= render "form", group: @group, post: @post, comment: @comment %>
  </div>
  <div>
    <%= link_to "Back to comments", group_post_comments_path(@group, @post) %>
  </div>
</div>
